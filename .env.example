# Event-only deployment key (with or without a 0x prefix; never commit a real key)
PRIVATE_KEY=your_private_key_here

# Sepolia RPC URL (you can get one from Alchemy, Infura, or other providers)
SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_KEY

# Optional: Ethereum mainnet archive-capable RPC for the real-token/feed fork test
MAINNET_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY

# Optional: reproducible fork block (defaults to the event-window proof block)
MAINNET_FORK_BLOCK=25905465

# Optional: Etherscan API key for contract verification
ETHERSCAN_API_KEY=your_etherscan_api_key_here

# Optional: WETH address for the AquaSwapVMRouter deployment
# (defaults to the canonical address on mainnet/sepolia, a WETHMock is deployed on local networks)
# WETH_ADDRESS=0x...
